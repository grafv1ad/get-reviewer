const GetResultButton = () => {
    const getResult = () => {

    }

    return (
        <button 
            className="get-result-button"
            onClick={getResult}
        >
            Get reviewers
        </button>
    );
}

export default GetResultButton;
